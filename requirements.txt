Django>=5.0,<5.2
psycopg[binary]>=3.1      # PostgreSQL (recomendado en producción)
gunicorn>=22.0            # servidor de aplicación
pyodbc>=5.0               # solo si Zeus es SQL Server y se consulta por ODBC
